#include <iostream>

using namespace std;

int main()
{  
     int n, i, factor = 0;
  cin >> n;
  for (i = 1; i <= n; i++)
    {
      if (n % i == 0)
	{
	  factor = factor + 1;
	}
    }
  if (factor == 2)
    {
      cout << "prime";
    }
  else
    cout << "not a prime";

}
